# Git identities are discovered from accounts authenticated with `gh`. A rule
# overrides the normal repository-owner default when the repository name
# starts with this text (case-insensitive).
[[git_user_defaults]]
repository = "work-project"
github_user = "work-user"
